import { RoleNameKeyPipe } from './role-name-key.pipe';

describe('RoleNameKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleNameKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
